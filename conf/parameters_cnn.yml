#data preprocessing:
preprocess:
  dimension:
    M:  434   # ultrasound image dimension
    N:  636   # ultrasound image dimension

#model
model:
  name: 'cnn'
  lr: 0.0006
  #momentum: 0.90
  step_size: 7
  threshold: 0.7
  gamma: 0.1
  epoch: 20
  batch_size: 32
  pretrained: 
  optimizer: 'adam'
  train_pct: 0.90
  patience: 4
  is_raw_data:  True # 1 if the input points are raw images and 0 if the input points are scattering features
  search_iter: 50
  is_rgb: False
  random_crop_size: 
  dropout: 0.35636296784442145
  kernel_size: 3
  transform: True
 
pca:
  n_components: None
  standardize: True

cross_val:
  seed: 11
  test_n_splits: 11
  
# kymatio 
scattering:
  J: None
  max_order: None

mlflow:
  tracking_uri: '/NOBACKUP/gauthiers/Chronic-Liver-Classification/mlruns'
  experiment_name: 'cnn_exp'

