#data preprocessing:
preprocess:
  dimension:
    M:  434   # ultrasound image dimension
    N:  636   # ultrasound image dimension

#model
model:
  name: 'resnet-18'
  lr: 0.00075
  #momentum: 0.90
  step_size: 7
  threshold: 
  gamma: 0.1
  epoch: 20
  batch_size: 32
  pretrained: True
  optimizer: 'adam'
  train_pct: 0.9
  patience: 10
  is_raw_data:  True # 1 if the input points are raw images and 0 if the input points are scattering features
  search_iter: 50
  random_crop_size: 224
  is_rgb: True
  dropout: 0.3856
  kernel_size:
  train_samples: 100


pca:
  n_components: 
  standardize: 

cross_val:
  seed: 42
  test_n_splits: 5
  
# kymatio 
scattering:
  J: 
  max_order: 

mlflow:
  tracking_uri: '/NOBACKUP/gauthiers/Chronic-Liver-Classification/mlruns'
  experiment_name: '04_C_resnet_18'

