#data preprocessing:
preprocess:
  dimension:
    M:  434   # ultrasound image dimension
    N:  636   # ultrasound image dimension

#model
model:
  name: 'resnet-18'
  lr: 0.00049
  #momentum: 0.90
  step_size: 7
  threshold: 0.7
  gamma: 0.1
  epoch: 20
  batch_size: 32
  pretrained: False
  optimizer: 'adam'
  train_pct: 0.90
  patience: 4
  is_raw_data:  True # 1 if the input points are raw images and 0 if the input points are scattering features
  search_iter: 30
  random_crop_size: 224
  is_rgb: True
  dropout: None
  kernel_size: None


pca:
  n_components: None
  standardize: None

cross_val:
  seed: 11
  test_n_splits: 11
  
# kymatio 
scattering:
  J: None
  max_order: None

mlflow:
  tracking_uri: '/NOBACKUP/gauthiers/Chronic-Liver-Classification/mlruns'
  experiment_name: 'test_resnet_18'

